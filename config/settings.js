// NaviOverlay Configuration
// Modify these settings for your deployment

module.exports = {
    // Token expiration in days
    // 0 = never expire (recommended for self-hosting)
    expires: 2,  // 2 = expire after 2 days (the one I used for the website)

    // Server port
    port: 3000,
};

